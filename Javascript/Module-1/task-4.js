const originalList = [0, 2, 4, 6, 8, 10, 12, 14, 16];

const twiceList = [];
for (let i = 0; i < originalList.length; i++) {
    twiceList.push(originalList[i] * 2);
}


const halfList = [];
for (let i = 0; i < originalList.length; i++) {
    halfList.push(originalList[i] / 2);
}